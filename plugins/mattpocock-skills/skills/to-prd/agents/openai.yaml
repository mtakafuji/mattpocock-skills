interface:
  display_name: "To Prd"
  short_description: "Turn the current conversation into a PRD and publish it to the project issue tracker — no interview, just synthesis o..."
  default_prompt: "Use to-prd."
policy:
  allow_implicit_invocation: false
