interface:
  display_name: "To Issues"
  short_description: "Break a plan, spec, or PRD into independently-grabbable issues on the project issue tracker using tracer-bullet verti..."
  default_prompt: "Use to-issues."
policy:
  allow_implicit_invocation: false
