interface:
  display_name: "To Spec"
  short_description: "Turn the current conversation into a spec and publish it to the project issue tracker — no interview, just synthesis ..."
  default_prompt: "Use to-spec."
policy:
  allow_implicit_invocation: false
