interface:
  display_name: "To Tickets"
  short_description: "Break a plan, spec, or the current conversation into a set of tracer-bullet tickets, each declaring its blocking edge..."
  default_prompt: "Use to-tickets."
policy:
  allow_implicit_invocation: false
