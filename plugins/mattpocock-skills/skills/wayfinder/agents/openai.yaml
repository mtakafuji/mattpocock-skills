interface:
  display_name: "Wayfinder"
  short_description: "Plan a huge chunk of work — more than one agent session can hold — as a shared map of investigation tickets on your i..."
  default_prompt: "Use wayfinder."
policy:
  allow_implicit_invocation: false
